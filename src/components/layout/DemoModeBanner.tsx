/**
 * Demo Mode banner — intentionally disabled for public ZELU surfaces.
 * Seed/demo accounts may still exist in the database for internal use,
 * but credentials and “Demo” copy must never appear on customer-facing pages.
 */
export async function DemoModeBanner() {
  return null;
}
